apiVersion: v1
kind: Secret
metadata:
  name: hpe-secrets
  namespace: hpe
  labels:
    app: hpe-pipeline
type: Opaque
data:
  # base64 of "hpe-dev-token" — fallback only, AppRole is primary auth
  # echo -n "hpe-dev-token" | base64 → aHBlLWRldi10b2tlbg==
  VAULT_TOKEN: aHBlLWRldi10b2tlbg==
